# Model Weights

Place your YOLOv11 segmentation weights here as `best.pt`.

This directory is excluded from version control. On Hugging Face Spaces,
commit the weights file directly (it will be stored in the Space's Git LFS).

The server defaults to `weights/best.pt`. Override with the `MODEL_PATH`
environment variable.
